# 15 прототипов — что строим вместо 62 фейковых анимаций

Райнур про старые плейсхолдеры сказал прямо: это не демонстрация ничего.
Решение: **15 настоящих кликабельных демо** — по одному на каждый из 12 типов
софта (`software-types.md`), плюс отраслевые. Посетитель не читает про
«сборщика прайсов» — он нажимает кнопку и смотрит, как строки слетаются в
таблицу.

**Формат каждого демо:** React-страница, строится Клодом по промпту, данные
фейковые и подписаны «демо-данные» (выдуманные данные в интерфейсе — можно,
выдуманные цифры о нас — нельзя). Встраивается в «Работы» как живая страница +
15-секундная запись экрана для карточки. Один промпт — один вечер — одно демо.

**Эталон уже есть:** аукцион автозапчастей с подключением к 1С (№1 ниже) —
настоящий, рабочий, у Дамира. Он задаёт планку формата: задача → потрогать →
что изменилось.

---

| # | демо | тип из 12 | кто узнаёт себя | что видно на экране |
|---|---|---|---|---|
| 1 | **Аукцион автозапчастей + 1С** *(есть, рабочий)* | Рабочее приложение + Мост | автобизнес, опт | лоты, ставки в реальном времени, сделка уезжает в 1С |
| 2 | **Сборщик прайсов** | Сборщик | опт, снабжение | три прайса (почта, сайт, Excel) → кнопка → одна таблица с лучшей ценой по позиции |
| 3 | **Бот-калькулятор** | Собеседник | все, кто продаёт по прайсу | окно Telegram: клиент пишет «нужно 40 листов», бот считает по прайсу и выставляет счёт |
| 4 | **Конвейер документов** | Конвейер документов | юристы, стройка, услуги | форма сделки → кнопка → готовые счёт+договор+акт в PDF |
| 5 | **Распознаватель накладных** | Распознаватель | склад, бухгалтерия | фото мятой накладной → строки появляются в таблице с подсветкой уверенности |
| 6 | **Сторож цен конкурентов** | Сторож | розница, опт | список конкурентов, у одного цена упала — алерт в TG с разницей |
| 7 | **Экран правды** | Экран правды | производство, логистика | live-дашборд цеха: заказы по этапам, красное там, где стоит |
| 8 | **Утренний отчёт владельцу** | Отчёт по расписанию | владельцы | телефон 7:00, в TG приходит: выручка вчера, касса, долги, три проблемы |
| 9 | **Предсказатель закупки** | Предсказатель | опт, производство | график остатков, прогнозная линия, «заказать до 14-го, иначе разрыв 6 дней» |
| 10 | **Витрина из 1С** | Публичная витрина | опт с базой в 1С | каталог с фильтрами, остатки честные, кнопка «в заявку» — всё из учётки |
| 11 | **Генератор рассылки** | Генератор контента | все с базой клиентов | сегмент «молчат 90 дней» → кнопка → персональные письма, каждое своё |
| 12 | **CRM логиста** | Рабочее приложение | логистика | воронка заявка→машина→в пути→закрыто, звонок прикреплён к сделке |
| 13 | **Пробив контрагента** | Мост | все b2b | ИНН → карточка: реквизиты, суды, долги, светофор риска |
| 14 | **Пакет юрфирмы** | отраслевой (Конвейер+Сторож) | юристы | документы из шаблонов + мониторинг судов по клиентам в одном окне |
| 15 | **Пакет стройки** | отраслевой (Экран+Конвейер) | стройка | этапы объекта, фотоотчёты с телефона прораба, акты по кнопке |

Покрытие: все 12 типов (№1–13) + два отраслевых пакета из каталога (№14–15).

---

## Порядок постройки

Первыми — те, что видны в услугах главной (эффект узнавания сильнее всего):
**2 сборщик → 3 бот → 12 CRM → 8 отчёт → 5 распознаватель**, дальше по таблице.
№1 уже есть — его оформить кейсом первым, раньше всех демо.

## Как строится одно демо (конвейер)

1. Промпт Клоду: «одна React-страница, тёмная палитра v2 (`--bg:#0e202d`,
   `--ink:#F2F7F5`, Nunito), демо-данные подписаны, одна главная кнопка,
   которая показывает магию за 10 секунд». Основа промпта — строка из таблицы.
2. Дамир смотрит, правит вкус (2–3 варианта на визуальные развилки).
3. Экспорт: статическая страница в `app/public/demo/<имя>/`, роут из «Работ».
4. Запись экрана 15с → карточка в «Работах» проигрывает её при наведении.

Правила: никакого бэкенда в демо — всё на фейковых данных в памяти; каждая
страница самодостаточна (не тянет ничего из V2.tsx, кроме палитры); вес демо
до 300KB, без библиотек анимации — CSS и SVG, как на остальном сайте.
